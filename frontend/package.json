{
  "dependencies": {
    "@extism/extism": "^2.0.0-rc11"
  },
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=build/bundle.js --loader:.wasm=file",
    "serve": "http-server build -o"
  },
  "devDependencies": {
    "esbuild": "0.25.4"
  }
}

